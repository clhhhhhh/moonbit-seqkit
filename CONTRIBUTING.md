# Contributing

1. Install MoonBit 0.10.4 or newer.
2. Run `moon fmt`, `moon check --deny-warn`, and `moon test --deny-warn`.
3. Keep public APIs dependency-free and document edge cases.
4. Use a focused commit message and include a regression test for behavior changes.
